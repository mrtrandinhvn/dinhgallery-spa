# Versioning

When preparing a pull request that contains a feature or bug fix, update the `version` in both `package.json` and `package-lock.json` using semantic versioning before opening the PR: increment the minor version for backward-compatible features and the patch version for backward-compatible bug fixes. Increment the major version only for breaking changes.
